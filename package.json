{
  "name": "my-node-app",
  "version": "1.0.0",
  "description": "My Node.js app",
  "main": "app.js",
  "scripts": {
    "test": "mocha tests/test.js",
    "build": "echo 'Build is complete!'"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
    // Add other dependencies if needed
  },
  "devDependencies": {
    "mocha": "^10.0.0"
    // Add other dev dependencies for testing, linting, etc.
  }
}
